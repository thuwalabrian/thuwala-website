# Example environment variables for Thuwala website
# Copy this file to `.env` and fill in real secrets before running in production.

# Flask environment: development or production
FLASK_ENV=development

# Secret keys (REQUIRED in production)
SECRET_KEY=replace-me-with-a-long-random-string
SECURITY_PASSWORD_SALT=replace-me-with-another-random-string

# Database
# For local dev (SQLite): sqlite:///thuwala.db
# For Postgres: postgresql://user:pass@host:port/dbname
DATABASE_URL=sqlite:///thuwala.db

# Email (SMTP) settings
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=you@example.com
MAIL_PASSWORD=supersecretpassword
MAIL_DEFAULT_SENDER=noreply@yourdomain.com
